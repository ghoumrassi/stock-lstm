from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Baseline model for stock price project.',
    author='Adam Ghoumrassi',
    license='MIT',
)
